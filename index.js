const names = ['Carlos Damian','Rodrigo Mata', 'Ivan Estrada','Joshua Ramos', "José Enrique"];

const printNames = (arr) => {
  arr.forEach((element) => {
    console.log(element);
  });
};

printNames(names);

